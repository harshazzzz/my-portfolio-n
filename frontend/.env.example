# Required: deployed backend HTTPS origin, without /api or a trailing path.
NEXT_PUBLIC_API_URL=
# Optional server-only override; takes precedence over NEXT_PUBLIC_API_URL.
BACKEND_URL=

# Optional canonical origin; defaults to the free Vercel domain.
NEXT_PUBLIC_SITE_URL=
